# Day 2 – standard labs
# ---------------------
# No Hugging Face token is required. All models used in this course are
# public; transformers/sentence-transformers download them anonymously.
# Do not ask students to create HF accounts or tokens for these labs.
#
# Copy to .env only if you later add optional keys (e.g. a different
# provider). For the shipped notebooks, .env can stay empty or absent.
OPENAI_API_KEY=